@model BookingPageViewModel


@if(Model.BookingPage.BookingPageItems.Count > 0)
{
    <li>
        <a>
<span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="card-status">
                @Model.BookingPage.BookingPageItems.Count
            </span>
        </a>
    </li>
}